services:
  api:
    build: .
    container_name: medical_saas_api
    ports:
      - "8000:8000"
    volumes:
      - ./medical_db:/app/medical_db
      - ./uploads:/app/uploads
      - ./data:/app/data
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"